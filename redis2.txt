127.0.0.1:6379
npm start

打开cmd
使用 e: 命令进入E：盘
使用cd redis 找到文件
使用 redis-server.exe redis.windows.conf 开启

再打开一个cmd 之前的不要关
找到文件 输入 redis-cli

auth 123 密码登入 

查询账户和密码 config get requirepass
使用 set设置
 名称=myKey 值=123

命令： set myKey 123

get myKey 拿值

keys * 查询所有键

expire 键=name 60 60秒自动删除

expire name 60

del 键=name 删除

退出 exit

redis-server.exe --service-install redis.windows.conf



